generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model CriptoCurrency {
  id    String @id @default(uuid())
  names String
  votes Int
}

model new_CriptoCurrency {
  id    String @id
  name  String
  votes Int
}
